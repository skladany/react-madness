import styled from "styled-components";
import { backgroundColor } from "./color-scheme";

const Round = styled.section`
  width: 20%;
  float: left;
  padding: 1em;
  background: ${backgroundColor};
`;

export default Round;
